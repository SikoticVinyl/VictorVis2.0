query GetTournaments($first: Int!, $after: String) {
    tournaments(
        first: $first
        after: $after
    ) {
        pageInfo {
            hasNextPage
            endCursor
        }
        edges {
            node {
                id
                name
                nameShortened
            }
        }
    }
}