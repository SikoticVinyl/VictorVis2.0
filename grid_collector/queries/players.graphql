query GetTeamRoster($teamId: ID!) {
    team(id: $teamId) {
        id
        name
        players: roster {
            id
            nickname
            title {
                id
                name
            }
            private
        }
    }
}